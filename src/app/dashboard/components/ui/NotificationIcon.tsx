"use client";
import { CiBellOn } from "react-icons/ci";

export default function NotificationIcon() {
  return (
    <CiBellOn
      size={24}
      style={{
        backgroundColor: "white",
        borderRadius: "20px",
        cursor: "pointer",
      }}
    />
  );
}
